# Configuración de variables de entorno para producción en Render

# Puerto que utilizará PocketBase (Render usa el puerto 10000 por defecto)
PORT=10000

# URL base de PocketBase (se establecerá automáticamente por Render)
# Ejemplo: https://tu-proyecto.onrender.com
# Esta variable se puede configurar en el panel de Render

# Configuración de CORS para permitir el acceso desde el frontend
# Reemplaza con la URL de tu frontend desplegado
ALLOWED_ORIGINS=https://tu-frontend.vercel.app,https://tu-frontend.netlify.app,http://localhost:3000

# Configuración de la base de datos (Render maneja los discos persistentes automáticamente)
PB_DATA=/pb/pb_data

# Configuración adicional de PocketBase
PB_ENCRYPTION_KEY=tu-clave-secreta-muy-segura-aqui